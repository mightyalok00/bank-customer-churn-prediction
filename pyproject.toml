[project]
name = "bank-customer-churn-prediction"
version = "1.0.0"
description = "Decision Tree and Random Forest churn prediction portfolio project"
requires-python = ">=3.12,<3.15"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
